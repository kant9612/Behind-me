<!DOCTYPE html>
<html>
  <head>
    <title>BehindMe</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!--font awesome の導入-->
    <script src="https://kit.fontawesome.com/cd1c031c48.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="header">
    <header class="">
      <nav class="navbar navbar-expand-lg">
        <div class="container">
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <%= link_to "Behind me", root_path, class: 'navbar-brand text-dark' %>
            <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>
              <li class="nav-item"><%= link_to "MyPage", user_path(current_user.id), class:'nav-link text-dark' %></li>
              <li class="nav-item"><%= link_to "Users", users_path, class: 'nav-link text-dark' %></li>
              <li class="nav-item"><%= link_to "Lists", lists_path, class: 'nav-link text-dark' %></li>
              <li class="nav-item"><%= link_to "logout", destroy_user_session_path, method: :delete, class: 'nav-link text-dark' %></li>
            <% else %>
              <li class="nav-item"><%= link_to "Home", root_path, class: 'nav-link text-dark' %></li>
              <li class="nav-item"><%= link_to "About", home_about_path, class: 'nav-link text-dark' %></li>
              <li class="nav-item"><%= link_to "sign up", new_user_registration_path, class: 'nav-link text-dark' %></li>
              <li class="nav-item"><%= link_to "login", new_user_session_path, class: 'nav-link text-dark' %></li>
            <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <div class="body">
      <p class="notice"><%= notice %></p>
      <%= yield %>
      </div>
    </div>
  </body>
</html>
